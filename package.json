{
  "name": "caixa-emprestimos-api",
  "version": "1.0.0",
  "description": "API para simulador de empréstimos CAIXA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prestart": "node scripts/clear-port.js",
    "check-port": "netstat -ano | findstr :3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "optionalDependencies": {
    "mssql": "^10.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}